version: '3'

services:
  openflight_client:
    container_name: openflight_client
    build:
      context: .
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - "8080:8080"
      - "35729:35729"  # for live reload
